# NoteBook v1.1.0 更新日志

## 版本信息
- **版本号**: 1.1.0
- **发布日期**: 2026-05-22
- **更新类型**: 功能增强

## 新增功能

### 1. 媒体文件支持
#### 图片导入
- ✅ 支持格式：JPG, JPEG, PNG, GIF, WebP, SVG
- ✅ 导入方式：
  - **复制粘贴**：从文件管理器复制图片，粘贴到编辑器
  - **直接拖入**：拖放图片文件到编辑器区域
  - **工具栏按钮**：点击 📷 按钮选择图片文件
- ✅ 内嵌显示：图片在编辑器中实时预览，支持缩放

#### 视频导入
- ✅ 支持格式：MP4, WebM, MOV
- ✅ 导入方式：
  - **复制粘贴**：从文件管理器复制视频，粘贴到编辑器
  - **直接拖入**：拖放视频文件到编辑器区域
  - **工具栏按钮**：点击 🎥 按钮选择视频文件
- ✅ 视频播放器：内嵌视频播放器，带控制条和进度条

### 2. 智能导出导入
#### 导出增强
- ✅ **自动检测**：导出时自动扫描笔记中的媒体引用
- ✅ **媒体文件夹**：如果笔记包含媒体，创建同名 `.media` 文件夹
- ✅ **文件复制**：将所有引用的媒体文件复制到 `.media` 文件夹
- ✅ **绑定清单**：生成 `media_manifest.json` 记录文件绑定关系

#### 导入增强
- ✅ **智能恢复**：导入时自动检查同名 `.media` 文件夹
- ✅ **文件复制**：自动复制媒体文件到应用数据目录
- ✅ **引用恢复**：通过 HTML 内容自动恢复媒体引用
- ✅ **独立管理**：导入成功后，媒体文件夹的增删改不影响应用

### 3. 编辑器增强
- ✅ **工具栏扩展**：新增图片和视频导入按钮
- ✅ **拖放支持**：支持文件拖放到编辑器
- ✅ **粘贴支持**：支持从剪贴板粘贴媒体文件
- ✅ **样式优化**：媒体元素圆角、阴影、响应式布局

## 技术变更

### 后端修改
#### `src-tauri/src/lib.rs`
- 新增 `import_media_file` 命令：从文件路径导入媒体
- 新增 `import_media_bytes` 命令：从字节数据导入媒体
- 增强 `export_tbook` 命令：支持媒体文件夹导出
- 增强 `import_tbook` 命令：支持媒体文件夹导入

#### `src-tauri/src/media.rs`
- 新增 `MediaBinding` 结构：媒体与笔记的绑定关系
- 新增 `MediaExportManifest` 结构：媒体导出清单
- 完善媒体文件处理函数

### 前端修改
#### `src/components/NoteRichEditor.vue`
- 集成 Tiptap Image 扩展：支持图片插入
- 添加拖放处理：`handleDrop` 函数
- 添加粘贴处理：`handlePaste` 函数
- 添加导入函数：`pickAndImportImage`、`pickAndImportVideo`
- 添加媒体样式：图片、视频的 CSS 样式

#### 依赖更新
- 使用 `@tiptap/extension-image`：图片扩展支持
- 使用 `@tauri-apps/plugin-dialog`：文件选择对话框

## 使用说明

### 导入媒体文件
1. **方式一**：复制文件 → 在编辑器中粘贴
2. **方式二**：拖放文件到编辑器区域
3. **方式三**：点击工具栏 📷（图片）或 🎥（视频）按钮

### 导出包含媒体的笔记
1. 点击左下角「导出 .tbook」按钮
2. 选择保存位置
3. 系统会自动生成：
   - `笔记名.tbook`：数据库文件
   - `笔记名.media/`：媒体文件夹（如果笔记包含媒体）

### 导入包含媒体的笔记
1. 点击左下角「导入 .tbook」按钮
2. 选择 `.tbook` 文件
3. 系统会自动：
   - 导入数据库内容
   - 检查同名 `.media` 文件夹
   - 复制媒体文件到应用目录
   - 恢复所有媒体引用

## 注意事项

### 文件管理
- 媒体文件存储在：`~/Library/Application Support/com.tal.notebook/media/`
- 导出时创建 `.media` 文件夹，与 `.tbook` 文件同级
- 导入后媒体文件独立管理，原文件夹可删除

### 兼容性
- **向前兼容**：v1.1.0 可导入旧版 `.tbook` 文件（无媒体文件夹）
- **向后兼容**：旧版可导入 v1.1.0 `.tbook` 文件（但会忽略媒体文件夹）

### 性能考虑
- 大文件（>50MB）可能影响导出导入速度
- 建议压缩大尺寸图片/视频后再导入
- 媒体文件数量无限制，但建议合理管理

## 已知问题
1. **SVG 动画**：部分 SVG 动画可能无法正常播放
2. **视频格式**：某些编码格式的 MP4 可能不兼容
3. **内存使用**：同时编辑大量媒体文件可能占用较多内存

## 未来计划
- 媒体库管理界面
- 媒体文件压缩优化
- 云备份媒体文件
- 媒体文件搜索功能

---

**升级建议**：建议所有用户升级到 v1.1.0，以获得完整的媒体支持功能。